## About Installation

```
composer install
npm install
npm run dev

#Copy .env.example into .env and edit what is needed
#You may also need to give some permissions for specific folders (storage/boostrap)

php artisan key:generate
php artisan migrate:fresh --seed
php artisan storage:link  
```

# JM
